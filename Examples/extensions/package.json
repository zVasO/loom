{
  "name": "loom-extension-examples",
  "private": true,
  "description": "Example Loom extensions and the tests of the window.loom SDK (docs/extensions.md).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit --allowJs --checkJs --strict --target es2022 --lib dom,es2022 loom.d.ts jira-board/app.js"
  }
}
